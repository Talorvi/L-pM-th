@model IEnumerable<LoopMoth.Models.Kategorie>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kategorie2.nazwa)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kategorie2.nazwa)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id_kategorii }) |
                @Html.ActionLink("Details", "Details", new { id = item.id_kategorii }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id_kategorii })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
